# Feature Spec -> HR Heatmap Completion
---

## Requirements
---
- Build a new heatmap that matches the general styling of hr heatmap
- Each cell should be a bar that expresses how many (up to 5) sessions were completed that week
    - bar design is kind of like a phone battery percentage filling up the battery icon - in a sense - but just as a bar
    - 5 indicates 100% - anything above 5 should create a gold outline on the cell
- Design should be the same button/slider that exists in act plot1 for baseline separation, switching through these two heatmaps on the same card.
- also fix the inability to toggle these plots in modal view.
